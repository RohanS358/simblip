// Starter program for a fresh DSA Lab block: a clean slate — just the
// scaffold every C++ program needs, no pre-baked algorithm to delete first.

export const DEFAULT_DSA_SOURCE = `#include <iostream>

int main() {

  return 0;
}
`
