'use client'

/**
 * Legacy tutorial panel component - superseded by the interactive animated cursor walkthrough system.
 */
export function TutorialPanel({
  pageId,
  onClose,
  initialCourseId,
}: {
  pageId?: string | null
  onClose?: () => void
  initialCourseId?: string
}) {
  return null
}
